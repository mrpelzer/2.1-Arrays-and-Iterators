# 📘 Array Manipulation Review Project  

## *Using push, pop, shift, unshift, sort, reverse, length & Math.random()*

## 🔷 Project Overview

In this project, you will practice manipulating JavaScript arrays using the most essential array methods.  
You will begin with an initial list of mobile apps and perform a series of transformations using:

- `push()`
- `pop()`
- `shift()`
- `unshift()`
- `sort()`
- `reverse()`
- `length`

You will ***update and display your results*** step-by-step in the output boxes provided in the HTML layout.  
This project will help you understand how arrays change as you add, remove, reorder, and retrieve values.

## 🎯 Project Objective

By completing this project, you will be able to:

- Create and initialize arrays  
- Add and remove items using fundamental array methods  
- Track how each method changes the array  
- Render results into the DOM  
- Sort and reverse values  
- Generate random values using `Math.random()`  
- Strengthen your understanding of core JavaScript operations used in real web applications  

## 📌 Project Requirements

### **JavaScript Tasks**

1. **Create an array** containing at least 6 app names.  
1. **Add 3 apps** to the end using `push()`.  
1. **Add 3 apps** to the beginning using `unshift()`.  
1. **Remove 2 items** from the end using `pop()` and display:  
   - `"The last item removed was <item>"`
1. **Remove 2 items** from the beginning using `shift()` and display:  
   - `"The first item removed was <item>"`
1. **Sort** the array alphabetically using `.sort()`.  
1. **Reverse** the array using `.reverse()`.  
1. Display the **total number of items** using `.length`.  

## ⚡ Challenge Requirement

9. Use `Math.random()` to display **one random item** from your array.

1. Display the icons of each social network icon in the array.Create a new array with the links.

1. Display the icons and underneath display the name of the social network. Use only the previousouly created arrays.

## 🖥️ Output Requirements

- Each step must update the proper **output box** in the provided HTML layout.  
- Displays must show the correct **current state of the array**, or the **item removed**, depending on the instruction.  

---

## 🧰 Technical Requirements

- Only the required array methods may be used.
- No advanced methods such as `map`, `filter`, or `reduce`.
- Code must run in the browser without errors.
